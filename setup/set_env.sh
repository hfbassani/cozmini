# ADB path is required for android devices if adb is not in the PATH
# Enter the path for the ADB platform-tools and uncomment de lines below.

# ADB_PATH=[ENTER your ADB platform-tools path here]
# export PATH=".:$ADB_PATH:$PATH"